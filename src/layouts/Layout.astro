---
import Navbar from "@components/Navbar.astro";
import Footer from "@components/Footer.astro";

interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="Diseño de páginas web 100% personalizadas para tu negocio o emprendimiento"
      content="Astro description"
    />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
  </head>
  <body>
    <div
      class="fixed inset-0 -z-10 h-full w-full items-center px-5 py-24 [background:radial-gradient(125%_125%_at_50%_10%,#ccdcf1_40%,#7fa8db_100%)] dark:[background:radial-gradient(125%_125%_at_50%_10%,#0e0e11_40%,#4360b2_100%)]"
    >
    </div>
    <Navbar />
    <div id="slot">
      <slot />
    </div>
    <div id="notAllow" class="pt-[10dvh] md:pt-[12vh] w-full py-10 px-10">
      <h2
        class="text-center font-semibold text-balance text-3xl md:text-5xl text-havelock-blue-950 dark:text-havelock-blue-100 py-28"
      >
        Esta página no está optimizada para navegadores basados en Firefox, se
        recomienda usar navegadores basados en Chrome, si continúas navegando
        podrás ver algunos elementos fuera de lugar.
      </h2>
      <button
        id="toggleFirefox"
        class="text-primary-dark dark:text-havelock-blue-100 bg-havelock-blue-400/60 hover:bg-havelock-blue-400 dark:hover:bg-havelock-blue-900 focus:outline-none font-medium rounded-full text-base px-5 py-2.5 text-center dark:bg-havelock-blue-900/80 hover:cursor-pointer mx-auto block mt-5"
        >Clic aquí para continuar</button
      >
    </div>
    <Footer />
  </body>
</html>
<style is:global>
  html {
    scroll-behavior: smooth;
    scrollbar-width: none;
  }
  ::-webkit-scrollbar {
    width: 8px;
    height: 8px;
  }

  ::-webkit-scrollbar-track {
    background: rgba(0, 0, 0, 0.15);
  }

  ::-webkit-scrollbar-thumb {
    @apply bg-neutral-500;
  }
  @media (prefers-reduced-motion) {
    * {
      animation: none;
    }
    html {
      scroll-behavior: auto;
    }
  }
</style>
<script>
  const regex = /firefox/i;
  const userAgent = navigator.userAgent;
  const slot = document.getElementById("slot");
  const notAllow = document.getElementById("notAllow");
  const toggleFirefox = document.getElementById("toggleFirefox");
  if (regex.test(userAgent)) {
    slot?.style.setProperty("display", "none");
    notAllow?.style.setProperty("display", "block");
  } else {
    slot?.style.setProperty("display", "block");
    notAllow?.style.setProperty("display", "none");
  }
  toggleFirefox?.addEventListener("click", () => {
    slot?.style.setProperty("display", "block");
    notAllow?.style.setProperty("display", "none");
  });
</script>
